pipeline {
    environment {
        DOCKER_HOST = "tcp://host.docker.internal:2375"
        DOCKER_TLS_VERIFY = "0"
    }
    agent {
        docker {
            image 'maven:3.9.8-eclipse-temurin-17'
            // ensures DNS for host works
            args '--add-host=host.docker.internal:host-gateway'
        }
    }
    stages {
        stage('Docker Check') {
            steps {
                sh 'echo DOCKER_HOST=$DOCKER_HOST'
                sh 'docker version'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }
    }
}
